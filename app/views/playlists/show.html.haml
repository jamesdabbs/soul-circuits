%h1= @playlist.title
%h2 By #{@playlist.user.username}

%img{ src: @playlist.photo_url }

%audio{ controls: 'controls' }
  %source{ src: @playlist.audio_url, type: 'audio/mp3' }
  Your browser does not support the 'audio' element
