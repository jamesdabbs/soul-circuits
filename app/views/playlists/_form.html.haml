= form_for playlist do |f|
  - if playlist.errors.any?
    %p Please correct the following errors
    %ul
      - playlist.errors.full_messages.each do |error|
        %li= error

  = f.label :title
  = f.text_field :title

  = f.submit
