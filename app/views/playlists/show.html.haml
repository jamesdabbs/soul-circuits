.breadcrumb
  = link_to 'Home', playlists_url
  = link_to @playlist.title, ''

.sidebar
  %img{ src: @playlist.photo_url, style: 'width: 100%' }

  %audio{ controls: 'controls' }
    %source{ src: @playlist.audio_url, type: 'audio/mp3' }

.content
  = render partial: 'form', locals: { playlist: @playlist }
